fun int main()
    var int x, result;
    x = 0;
    dowhile
        print(x);
        x = x + 1
    enddo x < 5;
    switch x
        case 5,
            result = 100
        case 10,
            result = 200
        default,
            result = 0
    endswitch;
    return(0)
endfun